{
  "name": "delta",
  "version": "0.1.0",
  "description": "bot discord codé en typescript",
  "main": "build/index.js",
  "license": "MIT",
  "scripts": {
    "compile": "rm -Rf build/ && yarn tsc",
    "start": "yarn compile && node ."
  },
  "dependencies": {
    "discord.js": "^12.5.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "typescript": "^4.2.2"
  }
}
